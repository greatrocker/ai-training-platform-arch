fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pyodbc==5.1.0
pydantic-settings==2.5.2
celery==5.4.0
redis==5.0.8
minio==7.2.9
httpx==0.28.1
pyyaml==6.0.2
python-multipart==0.0.9
# torch/torchvision come from the pytorch/pytorch base image (CUDA 12.1
# build matching this host's GPU) — ultralytics only needs to be satisfied
# by what's already installed, not pull its own CPU wheel from PyPI.
ultralytics==8.4.155
